Programa expresiones;
var
  int A[4], B[5][4];
  int a, b, c, f, g, h;
  float d, e;
  bool k, l, m;

funcion int fact (int j):
var int x;
{ 
  escribe("FACT");
  escribe(j);
  si ((j > 0)) entonces {
    j = j - 1;
    regresa(fact(j));
  } sino {
    regresa(-2);
  }
}

funcion void inicia(int y, float z):
var 
  int x;
  float s;
  bool m, d, r;
{
  %% x = 5;
}

principal() {
  A[0] = 30;
  A[1] = 20;
  A[0] = A[1];
  escribe(A[0]);
}